rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // 테스트를 위해 로그인한 사용자에게 모든 권한 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

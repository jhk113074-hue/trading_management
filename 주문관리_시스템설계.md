# YSACC 무역/주문 관리 시스템 설계서 (최종 개정본)
## `portal.ysacc.co.kr/orders` 기반 시스템 아키텍처 및 화면 재구성

본 문서는 YSACC 주문관리 시스템의 화면 흐름, 주요 기능적 컴포넌트, 공통 UI 규칙, 그리고 Firebase Firestore 데이터 모델 명세를 최신 구현체에 맞춰 체계적으로 정리한 시스템 설계서입니다.

---

## 📑 목차

1. [시스템 전체 아키텍처](#1-시스템-전체-아키텍처)
2. [대시보드 (Dashboard)](#2-대시보드-dashboard)
3. [오더 상세 페이지 및 공통 UI 레이아웃](#3-오더-상세-페이지-및-공통-ui-레이아웃)
4. [단계별 워크플로우 명세 (Tabs 1-4)](#4-단계별-워크플로우-명세-tabs-1-4)
   - [탭 1: PO 접수 (발주 관리)](#탭-1-po-접수-발주-관리)
   - [탭 2: 소싱 발주 (매입 및 계약)](#탭-2-소싱-발주-매입-및-계약)
   - [탭 3: 선적 관리 (부킹/통관/물류)](#탭-3-선적-관리-부킹통관물류)
   - [탭 4: 정산 마감 (이익 관리)](#탭-4-정산-마감-이익-관리)
5. [데이터 모델 명세 (Firestore Schema)](#5-데이터-모델-명세-firestore-schema)

---

## 1. 시스템 전체 아키텍처

### 1.1 화면 흐름 및 네비게이션
```
┌────────────────────────────────────────────────────────┐
│                      [대시보드]                        │
│   - 실시간 오더 현황판 / 총 거래액                     │
│   - 다음 할일 (기한 임박 및 미결 서류 자동 경고 알림)   │
└──────────────────────────┬─────────────────────────────┘
                           │ (오더 카드 클릭 시 이동)
                           ▼
┌────────────────────────────────────────────────────────┐
│                      [오더 상세]                       │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 1. 오더 요약 바 (고객사, 납기, 현황 한눈에 보기)   │  │
│  ├─────────────────────────┬────────────────────────┤  │
│  │                         │                        │  │
│  │ 2. 단계별 워크플로우 탭 │ 3. 고정 사이드 패널    │  │
│  │    [PO접수/발주]        │    - 실시간 손익 계산  │  │
│  │    [소싱발주]           │    - 수주품목 명세     │  │
│  │    [선적관리]           │    - 타임라인 변경이력 │  │
│  │    [정산마감]           │                        │  │
│  │                         │                        │  │
│  └─────────────────────────┴────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

### 1.2 핵심 설계 원칙
* **진행 단계의 비강제성 (유연성)**: 프로세스(발주 → 선적 → 이익)의 흐름을 가이드라인으로 제공하되, 이전/이후 단계의 탭 이동 및 정보 조정을 차단하지 않아 실무 편의성을 극대화합니다.
* **이중화된 서류 관리 (하이브리드 워크플로우)**:
  * **자동 생성(인쇄)**: 시스템에 입력된 정보를 파싱하여 표준 규격(CI, PL, 도착보고서, 쉬핑마크)을 웹 프린트 템플릿 창으로 즉시 렌더링 및 PDF 인쇄를 지원합니다.
  * **수동 보관(파일 업로드)**: 실제 포워더나 공급사로부터 서명받은 원본 파일들을 드래그 앤 드롭으로 각 항목별 storage 컬렉션에 유첨합니다.
* **자동화된 데이터 바인딩**: PI(프로포마 인보이스) 연동 시점에 고객정보, 단가, 품목 정보가 자동으로 수입/매칭되어 중복 입력을 제거합니다.

---

## 2. 대시보드 (Dashboard)
* **주요 기능**:
  * **실시간 요약 지표**: 진행 중인 총 오더 수, 당월 수주/매입 누적 총액, 평균 이익률 표시.
  * **다음 할일 (Action Items) 알림 알고리즘**:
    * L/C 결제건의 선적 서류 마감 임박 여부
    * 필수 품질 서류(COA, Test Report) 미수취 알림
    * 선적 정보 입력 단계에서 포워더 또는 Vessel 부킹 정보가 공란인 경우 경고 아이콘 표시

---

## 3. 오더 상세 페이지 및 공통 UI 레이아웃

### 3.1 2단 분할 그리드 구조
* **좌측 영역 (60% 너비)**: 현재 선택된 프로세스 탭의 상세 입력/관리 화면이 렌더링됩니다.
* **우측 영역 (40% 너비 - 스크롤 고정)**: 
  * **실시간 이익현황 피드**: 환율 변동 및 포워딩 운송비, 세금 정보가 반영된 실시간 영업이익과 마진율 계산기가 노출됩니다.
  * **수주 품목 목록**: 오더에 등록된 품목명, 스펙, 원물가(KRW/USD) 및 수량이 표시됩니다.
  * **변경 히스토리 타임라인**: Firestore 변경 이력을 추적하여 "누가, 언제, 어떤 데이터"를 변경했는지 로그를 피드 형태로 시각화합니다.

### 3.2 UI 디자인 가이드라인
* **테마**: YSACC 고유의 프리미엄 Navy 아이덴티티 (`rgb(30, 58, 95)`)를 주요 헤더 및 버튼 스타일로 사용합니다.
* **그리드 레이아웃**: 모든 리스트 및 매핑 리스트는 2줄 복합 격자 레이아웃을 사용하여 가독성과 정렬 정밀도를 확보합니다.

---

## 4. 단계별 워크플로우 명세 (Tabs 1-4)

### 탭 1: PO 접수 (발주 관리)
* **정보 매핑**: 수주된 오더에 대해 품목별 **공급업체 지정**, **실구매단가(KRW)**를 매핑하여 원가를 확정합니다. (품목코드, 공급사명, 스펙 중심의 인보이스 형태 뷰어 탑재. 불필요한 Grade, 이메일은 화면에서 생략 가능)
* **도착보고서 자동생성**:
  * CFS 입고를 위한 **[🚚 도착보고]** 버튼을 통해 CFS 주소, 담당자 연락처, 입고예정일, 품목 정보가 바인딩된 운송/CFS 제시용 도착보고 양식을 팝업 화면으로 자동 출력합니다.
* **쉬핑마크 자동생성**:
  * 공급사에 전달하여 포장 박스/팔레트에 부착할 **[🏷️ 쉬핑마크]**를 표준 양식(YSACC 다이아몬드 마크, 포트명, 원산지, 카톤 정보)에 맞추어 실시간 생성하고 프린트/PDF 저장을 수행합니다.
* **수정 및 편집 기능**: PO접수 상태에서도 전체적인 주문 정보(수량, 단가, 비고)의 수정, 삭제 및 변경이 언제든 자유롭게 가능하도록 컨트롤러를 탑재합니다.

### 탭 2: 소싱 발주 (매입 및 계약)
* **공급업체별 카드 분할**: 공급업체별로 별도의 발주 카드가 자동 분할 생성됩니다.
* **세무 및 계약 관리**:
  * **과세구분 설정**: 공급사별 공급 유형에 따라 `과세` / `영세`를 구분하여 세금 계산서 발행 여부를 체크합니다.
  * **구매확인서(Local L/C) 발송**: 업체별로 구매확인서 발급 유무(`supplierPurchaseCertificate`)를 상태값으로 지정하고 관련 증빙서류(`supplierPurchaseCertFiles`)를 수동 업로드합니다.
  * **대금 분할 지급 스케줄**: 3~4차에 걸쳐 순차 지급되는 분할 결제 일정(날짜 및 금액)을 그리드 형태로 입력/보관합니다.

### 탭 3: 선적 관리 (부킹/통관/물류)
* **물류 운송 및 선적 정보 입력**:
  * 포워더 목록(다중 포워더 견적 비교 및 선정 내역 저장), Vessel명/항차 정보, 컨테이너 종류 및 수량(Volume & Quantities), 수출신고번호, CFS/Door 작업 형태 선택, 면장 적용 환율을 입력합니다.
* **CI/PL 하이브리드 관리**:
  * **자동 생성**: `[CI 자동인쇄/생성]` 및 `[PL 자동인쇄/생성]`을 통해 시스템 데이터 기반으로 Commercial Invoice & Packing List를 즉시 생성 및 출력합니다.
  * **수동 유첨**: 바이어 또는 선사로부터 확정 서명된 원본 서류(`ciFiles`, `plFiles`)를 드래그 앤 드롭 업로드하여 통합 아카이빙합니다.
* **현장 사진 및 물류 증빙 첨부**:
  * CFS/작업장 내 **컨테이너 내부/외부 적재 및 씰(Seal) 작업 사진**(`containerWorkFiles`) 업로드 위젯 제공.
  * 차량 적재 상태 및 운송 과정을 확인할 수 있는 **운송 사진**(`transportationFiles`) 업로드 위젯 제공.

### 탭 4: 정산 마감 (이익 관리)
* **정산 확정 및 손익 대조**:
  * 수주 매출(AR)과 최종 매입가(AP) 및 다중 포워더 운송비용을 총합하여 정산 완료 처리합니다.
  * 최종 실현 손익 및 마진율이 사이드바의 실시간 이익현황 계산기에 고정 표출되어 신속한 의사결정을 돕습니다.

---

## 5. 데이터 모델 명세 (Firestore Schema)

```typescript
export interface Order {
  id: string;                               // PO 번호 (PO-YYYY-NNNN)
  custPo: string;                           // 고객사 PO 번호
  customer: string;                         // 고객사명
  manager: string;                          // YSACC 담당자명
  status: "주문" | "발주" | "선적관리" | "이익관리";  // 4단계 프로세스 진행 상태
  items: OrderItem[];                       // 상세 품목 목록

  // 선적 기본 및 세부 일정 정보
  vesselBooking?: string;                   // Vessel 명 / 항차
  etd?: string;                             // ETD (선적지 출발 예정일)
  eta?: string;                             // ETA (도착지 도착 예정일)
  exportDeclarationNo?: string;             // 수출신고번호
  containerWorkspaceType?: 'CFS' | 'Door' | ''; // 콘테이너 작업 장소

  // 물류 운송 비용 정보 (포워더 다중화)
  forwarders?: ForwarderEntry[];            // 다수 포워더 견적 및 확정 금액 배열

  // 공급업체별 세무 / 계약 정보
  supplierTaxTypes?: Record<string, '영세' | '과세'>; // 공급업체별 과세 구분
  supplierPurchaseCertificate?: Record<string, 'Y' | 'N' | ''>; // 구매확인서 발행 상태
  supplierPurchaseCertFiles?: Record<string, Attachment[]>;    // 구매확인서 서류 파일
  supplierPaymentInstallments?: Record<string, Installment[]>; // 분할 결제 정보

  // 이중화 서류 관리 (수동 업로드 파일 저장소)
  ciFiles?: Attachment[];                   // 상업 송장(Commercial Invoice) 파일
  plFiles?: Attachment[];                   // 패킹 리스트(Packing List) 파일
  cooFiles?: Attachment[];                  // 원산지 증명서(COO) 파일
  blFiles?: Attachment[];                   // 선하증권(B/L) 파일

  // 현장 사진 및 물류 증빙용 유첨 필드
  containerWorkFiles?: Attachment[];        // 컨테이너 작업 사진 목록
  transportationFiles?: Attachment[];       // 차량 상차 및 운송 사진 목록
}

interface OrderItem {
  itemId: string;                           // 품목 식별 고유키
  name: string;                             // 품목명
  supplier: string;                         // 매핑된 공급사명
  qty: number;                              // 수량
  unitPrice: number;                        // 수주단가
  purchaseUnitPrice?: number;               // 실매입단가 (원가 계산의 기초)
}

interface ForwarderEntry {
  name: string;                             // 포워더명
  freightAmount?: number;                   // 운송 비용
  freightCurrency?: 'USD' | 'KRW';          // 결제 통화
}

interface Attachment {
  name: string;                             // 파일명
  url: string;                              // 다운로드 가능한 스토리지 URL
  size: number;                             // 파일 용량 (bytes)
  path: string;                             // 스토리지 상의 참조 경로
}

interface Installment {
  date: string;                             // 결제 예정일
  amount: number;                           // 결제 금액
}
```
